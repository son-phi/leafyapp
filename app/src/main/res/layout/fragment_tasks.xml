<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F9FA">

    <!-- 1. LỊCH TUẦN (Header - Stick Top) -->
    <LinearLayout
        android:id="@+id/layout_calendar_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#FFFFFF"
        android:paddingBottom="12dp"
        android:elevation="4dp">

        <TextView
            android:id="@+id/tv_month_year"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="November 2025"
            android:padding="16dp"
            android:textStyle="bold"
            android:textColor="#000000"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_calendar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />
    </LinearLayout>

    <!-- 2. DANH SÁCH TASK (Scrollable) -->
    <!-- Nằm dưới Header và trên Footer -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_tasks"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/layout_calendar_header"
        android:paddingTop="16dp"
        android:clipToPadding="false"
        android:paddingBottom="100dp" />

    <!-- 3. EMPTY STATE -->
    <LinearLayout
        android:id="@+id/layout_no_task"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp"
        android:visibility="gone">

        <ImageView
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@android:drawable/ic_popup_reminder"
            app:tint="#BDBDBD"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="You Have No Task For Today"
            android:textColor="#000000"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginTop="16dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Add a new care reminder and carry out your daily plant care routines"
            android:gravity="center"
            android:textColor="#757575"
            android:layout_marginTop="8dp"/>
    </LinearLayout>

    <!-- 4. NÚT ADD TASK (Stick Bottom Right) -->
    <Button
        android:id="@+id/btn_add_task"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="80dp"
        android:text="Add Task"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:backgroundTint="#2DDA93"
        android:paddingHorizontal="24dp"
        android:paddingVertical="12dp"
        android:textAllCaps="false"
        app:cornerRadius="24dp"/>

</RelativeLayout>